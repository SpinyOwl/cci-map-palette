# CCI Map Palette

Minimal Architectury-based mod scaffold for the runtime FTB Chunks palette work.

## Layout

- `common/` contains shared code.
- `fabric/` contains the Fabric entrypoint.
- `forge/` contains the Forge entrypoint.

## Build

```sh
.\gradlew.bat build
```
