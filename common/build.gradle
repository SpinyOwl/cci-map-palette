dependencies {
	modImplementation "net.fabricmc:fabric-loader:${rootProject.fabric_loader_version}"
	modApi "dev.architectury:architectury:${rootProject.architectury_version}"
}

architectury {
	common("forge", "fabric")
}

configurations {
	dev
}

loom {
	accessWidenerPath = file("src/main/resources/cci_map_palette.accesswidener")
}

artifacts {
	dev(jar)
}
